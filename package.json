{
  "name": "aurora-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix aurora-web --",
    "build": "npm run build --prefix aurora-web",
    "start": "npm run start --prefix aurora-web",
    "lint": "npm run lint --prefix aurora-web"
  }
}
